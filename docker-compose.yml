services:
  expense-tracker:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      # Datenbankdatei außerhalb des Containers persistieren
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
